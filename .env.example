# Docker Compose 环境变量配置
# 复制此文件为 .env 并根据实际情况修改

# ============================================
# 部署目录配置
# ============================================
BASE_DIR=/www/wwwroot/mail.xg.eryun.top

# ============================================
# 应用配置
# ============================================
APP_NAME=在线邮箱客户端

# ============================================
# JWT 密钥配置
# ============================================
# 重要：生产环境必须修改此密钥！
# 建议使用随机生成的强密钥，例如：
# python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=JJAJLUUriXU830HwgizT0BEjS_h2LV62-kRPFcgfJ6U

# ============================================
# 管理员账号配置
# ============================================
# 初始管理员账号配置（仅首次部署时生效）
# 如果数据库中已存在该用户名的管理员，则不会重复创建
ADMIN_ACCOUNT=eryuns
ADMIN_PASSWORD=Hujunhao.110

# 管理员密码重置（用于忘记密码的情况）
# 设置此变量后，容器启动时会强制重置第一个管理员账号的密码
# 重置成功后请立即清空此变量，避免每次启动都重置密码
# RESET_ADMIN_PASSWORD=NewPassword123!
